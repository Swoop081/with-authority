# v0.9.153 Draw, Search and Choice Certification

The browser-prompt chooser has been replaced by a locked in-game overlay. Cancelling a required choice is impossible and cancel no longer silently selects the first page. Legal targets are filtered before display and revalidated at confirmation. Follow-up costs and effects occur only after a valid choice. CPU choices use the same filtered target lists. Empty searches resolve cleanly without a stall. Building Momentum supports an exact 1–3 page count followed by individual legal Momentum selections. Match end clears any pending chooser.

19 recovered choice-effect paths were converted. JavaScript syntax validation passed.
