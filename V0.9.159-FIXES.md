# v0.9.159

- Play pile uses the original move type (for example Mad Rush), never the resulting position (Grounded).
- Normal and submission-escape CPU Autocounters require a concrete retained follow-up outside emergencies.
- A concrete follow-up is either an immediately playable Move or an exact one-Momentum-then-Move sequence.
- Rarity stars appear at the top-right of the title bar on both faces.
